const initialState = {
	gameMode: null,
	playerName: "",
	playerChoice: "",
	opponentChoice: "",
	playerScore: 0,
	opponentScore: 0,
};

const state = { ...initialState };

export { state, initialState };
